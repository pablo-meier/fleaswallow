(* We don't want to rebuild the whole site every time, so we keep a .battletoads
 * directory wherever you're invoking the binary to compare timestamps. Here's
 * where we provide an API for what/where to update.*)



